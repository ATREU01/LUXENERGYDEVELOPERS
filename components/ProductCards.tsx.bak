import Image from "next/image"
import { products } from "@/lib/products"
export default function ProductCards(){
  return (
    <section id="storage" className="border-t border-line py-16">
      <div className="max-w-[1200px] mx-auto px-6">
        <h2 className="text-3xl font-bold">Choose your build.</h2>
        <p className="text-muted">Mix-and-match batteries, inverters, and accessories.</p>
        <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4 mt-4">
          {products.map(p => (
            <article key={p.slug} className="bg-card border border-line rounded-2xl p-4 shadow-lux flex flex-col">
              <div className="relative h-64 rounded-xl overflow-hidden border border-line">
                <Image src={p.imageUrl} alt={p.name} fill className="object-contain" />
              </div>
              <h3 className="mt-4 font-semibold">{p.name}</h3>
              <p className="text-muted">{p.summary}</p>
              <div className="flex gap-2 flex-wrap my-2">
                {p.tags.map(t => <span key={t} className="text-sm border border-line rounded-full px-2 py-1">{t}</span>)}
              </div>
              <div className="mt-auto flex items-center justify-between">
                <span className="text-muted">From ${(p.price/100).toFixed(0)}</span>
                <a href="#contact" className="px-3 py-1.5 rounded-full border border-accent shadow-lux">Quote</a>
              </div>
            </article>
          ))}
        </div>
        <p className="text-muted text-sm mt-2">Pricing for illustration only. White-label your preferred SKUs here.</p>
      </div>
    </section>
  )
}

